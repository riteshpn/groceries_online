import 'package:flutter/material.dart';

class PoductDetails extends StatelessWidget {
  static const routeName = "/ProductDetails";

  const PoductDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
